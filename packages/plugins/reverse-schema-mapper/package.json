{
  "name": "@magidoc/plugin-reverse-schema-mapper",
  "description": "A library that provides the ability to do a reverse lookup on GraphQL types.",
  "private": false,
  "version": "0.0.19",
  "type": "module",
  "license": "MIT",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "esbuild --minify --sourcemap --outdir=dist ./src/*.ts ./src/**/*.ts && tsc --build tsconfig.build.json",
    "test": "jest",
    "release": "pnpm build && pnpm publish"
  },
  "dependencies": {
    "graphql": "^16.3.0",
    "lodash": "^4.17.21",
    "prettier": "^2.5.1"
  },
  "devDependencies": {
    "@types/glob-to-regexp": "^0.4.1",
    "@types/jest": "^27.4.1",
    "@types/lodash": "^4.14.179",
    "@types/prettier": "^2.4.4",
    "esbuild": "^0.14.23",
    "graphql-query-compress": "^1.2.4",
    "jest": "^27.5.1",
    "jest-extended": "^2.0.0",
    "ts-jest": "^27.1.3",
    "typescript": "^4.6.2"
  },
  "peerDependencies": {
    "graphql": "^16.3.0"
  }
}
