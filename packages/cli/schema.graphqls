type Query {
    # non-null input, which contains a nullable sub-field. Eventually, the generator should generate null.
    inputs(target: FirstLevelTarget!, scalar: Damn): Boolean
}

input FirstLevelTarget {
    second: SecondLevelTarget
}

input SecondLevelTarget {
    first: FirstLevelTarget!
}

scalar Damn 